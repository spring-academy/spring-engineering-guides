#!/bin/bash
set -e

# Note: This directory structure reflects the Educates staged workshop content files.
# This does not reflect the directory structure the content git repository.
#
# As a content developer, you will not execute this script directly,
# as it is executed in context of a staged codebase in the educates
# workshop session.

# ~/exercises/scripts/stage-codebase.sh
